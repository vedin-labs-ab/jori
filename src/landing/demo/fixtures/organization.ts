/** The workspace every mock belongs to. The logo is served the way a real
 *  organization's would be, so the console's switcher shows it through the
 *  same path a real one takes. */
export const organization = {
  logo: "/demo/copperline.svg",
  name: "Copperline",
}
