// Fixture times are offsets from the workspace's own `now`, so the relative
// times the views print read the same on the server and on the client.

export const minute = 60_000
export const hour = 60 * minute
export const day = 24 * hour
