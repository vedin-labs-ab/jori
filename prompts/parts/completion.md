Finish the run by calling `finish_run`.

{% if tools.send_reply %}
Ending the run needs no closing message — when nothing is left to say, just finish, and pass an internal `reason` if you haven't sent anything visible.
{% endif %}
